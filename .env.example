# Documentation template only: this legacy application does NOT load .env files.
# Set this variable in the PHP process/web-server environment before using
# official-account creation or reset. Choose a private, local-only value.
DEFAULT_OFFICIAL_PASSWORD=replace-with-a-private-local-only-value
